pipeline {
  agent any

  stages {
    stage ('Build') {
      steps {
        sh "echo Run a command"
     }
   }
    stage ('test') {
      steps {
        sh "echo Run a command"
      }
    
    }
  }
}
