pipeline {
  agent any

  stages {
    stage ('Build') {
      steps {
        sh "echo Run a command"
     }
   }
    stage ('Stage') {
      steps {
        sh "sleep 20"
      }  
    }    
    stage ('test') {
      steps {
        sh "echo Run a command"
      }
    
    }
  }
}
